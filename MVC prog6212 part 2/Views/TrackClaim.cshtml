@page 
@model IEnumerable<MVC_prog6212_part_2.Models.Claim>
@{
    ViewData["Title"] = "Track Claims";
}

<h2>Track Your Claims</h2>

<table class="table">
    <thead>
        <tr>
            <th>Hours Worked</th>
            <th>Hourly Rate</th>
            <th>Additional Notes</th>
            <th>Supporting Document</th>
            <th>Status</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var claim in Model)
        {
            <tr>
                <td>@claim.HoursWorked</td>
                <td>@claim.HourlyRate</td>
                <td>@claim.AdditionalNotes</td>
                <td><a href="@claim.SupportingDocumentPath">Download</a></td>
                <td>@claim.Status</td>
            </tr>
        }
    </tbody>
</table>